query getPaymentMethods($customerId: String!){
    getPaymentMethods(customerId: $customerId) {
        id,
        token,
        number,
        expMonth,
        expYear,
        number,
        isDefault,
        brand
    }
}
